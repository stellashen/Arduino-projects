// Function prototypes for assembly file.
extern "C" {
  byte divide(byte a, byte b);
  int sumArray(byte a[], byte length);
  int avgArray(byte a[], byte length);
  int dot(byte a[], byte b[], byte length);
}
